const reducer = (state = {}, action) => {
  switch (action) {
    case "OPEN_EDIT_MODAL":
      return;
    case "CLOSE_EDIT_MODAL":
      return;
    default:
      return state;
  }
};

export default reducer;
